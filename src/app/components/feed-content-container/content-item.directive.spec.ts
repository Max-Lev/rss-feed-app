import { ContentItemDirective } from './content-item.directive';

describe('ContentItemDirective', () => {
  it('should create an instance', () => {
    const directive = new ContentItemDirective();
    expect(directive).toBeTruthy();
  });
});
