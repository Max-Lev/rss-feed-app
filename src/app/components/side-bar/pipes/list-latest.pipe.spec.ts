import { ListLatestPipe } from './list-latest.pipe';

describe('ListLatestPipe', () => {
  it('create an instance', () => {
    const pipe = new ListLatestPipe();
    expect(pipe).toBeTruthy();
  });
});
